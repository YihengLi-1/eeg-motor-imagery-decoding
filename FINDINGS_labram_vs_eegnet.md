# LaBraM vs EEGNet:cross-subject 运动想象(左/右手)对比 — 最终结论

**数据**:PhysioNet eegbci,runs [4,8,12],64 通道。**协议**:按被试切(防 leakage),
训练池 + 5 验证被试(早停)+ 10 测试被试(模拟新用户),每个测试被试单独算 acc/kappa,
报 10 人的 mean±std。**两个模型/三种用法、两个频段,其余口径完全一致,唯一变量是频段。**

## 核心结果表

### 划分 A(主划分,step3 + step3b,完整跑完)

| 模型用法 | 8–30Hz(窄带) | 0.1–75Hz(宽带) |
|---|---|---|
| EEGNet 端到端 | 49.6% ± 6.5% | —(本划分未跑) |
| LaBraM 线性探针 | 50.9% ± 4.2% | 54.2% ± 4.9% |
| **LaBraM 全量微调** | 50.0% ± 7.2% | **78.4% ± 16.4%**(kappa +0.58) |
| 随机猜测 | 50.0% | 50.0% |

### 划分 B(多 seed 矩阵的 seed 0,轻量配置跑完;全量微调未完成)

| 模型用法 | 8–30Hz | 0.1–75Hz |
|---|---|---|
| EEGNet 端到端 | 50.2% | **55.6%** |
| LaBraM 线性探针 | 49.3% | 52.0% |

### 综合定性结论(两个划分一致)

| | 窄带 8–30Hz | 宽带 0.1–75Hz | 变化 |
|---|---|---|---|
| EEGNet 端到端 | ~50% | ~55–56% | +5%(小) |
| LaBraM 线性探针 | ~50% | ~52–54% | +3%(小) |
| **LaBraM 全量微调** | ~50% | **~78%** | **+28%(质变)** |

## 三个结论

1. **窄带(MI 标准的 8–30Hz)下,所有模型都≈瞎猜。** 包括 2024 最新的脑电大模型 LaBraM。
   这复现了用户 README 里 EEGNet 撞的那堵 cross-subject 墙(~52%)。

2. **换成 LaBraM 预训练用的宽带(0.1–75Hz),LaBraM 全量微调跳到 ~78%——翻过了墙。**
   8/10 个没见过的新被试解码良好(67–100%,两个接近满分),只有 2 个仍在瞎猜。
   验证集(另一组独立未见被试)也独立涨到 ~72%,说明是真泛化,不是测试集走运。

3. **这个胜利来自 LaBraM 的大规模预训练,不只是"宽带信息多"。** 同样喂宽带,从零训练的
   EEGNet 只到 ~55.6%,而 LaBraM 全量微调到 ~78%。宽带对谁都有点帮助(+5%),但只有预训练
   的大模型能把它转化成质变(+28%)。→ **验证了 README 的猜想:大规模预训练能救 cross-subject。**

## 方法学教训(这条很值钱)

**一个看着完全合理的预处理选择(8–30Hz,MI 的领域标准),差点让我们得出"最新大模型也没用"的
错误结论——其实 LaBraM 只是需要它预训练时的频段。评估基础模型时,预处理能整个翻盘。**

## 诚实的 caveat(不能不写)

1. **78% 来自单次划分(划分 A,10 个测试被试)。** 本想用 3–4 个 seed 重新划分确认稳不稳,
   但全量微调这一步在后台反复被杀(排除了合盖睡眠——盖子开着也死;最可能是机器内存压力/
   长时间重负载下后台进程被杀),多 seed 验证未完成。**所以 78% 是强但单划分的结果,待多 seed 确认。**
2. **方差大(±16.4%)**:8/10 个新被试很好,2 个仍瞎猜。不是人人都行。
3. **全量微调过拟合训练集**(train acc 99.5%),但仍能泛化(独立验证集证实)——容量大要小心,
   这里靠早停 + 预训练先验救住了。
4. **EEGNet 宽带对照来自划分 B**(与 78% 不同划分);同划分的完整对照未跑完。但两划分在共有配置上
   表现一致(窄带都 ~50%、探针宽带都 ~52–54%),支持上面的定性结论。

## 复现文件
- `step3_compare.py` / `step3_results.json` — 划分 A 窄带三模型
- `step3b_broadband.py` / `step3b_broadband_results.json` — 划分 A 宽带 LaBraM
- `step3c_fullmatrix_results.json` — 划分 B(seed 0)部分结果
- `step3d_matrix.py` — 进程隔离 + 断点续跑版(多 seed 未完成);数据缓存 `_cache_*.pkl`
- 环境:`~/bci-project/venv`(py3.12,torch2.12+MPS,braindecode1.5.1)
- LaBraM 三个坑的解法:输入 ×1e6 转 µV;temporal_embedding 裁切移植到 1000 点;InterpolatedLaBraM 通道插值
